{% extends 'layouts/index.twig' %}

{% block head %}
    <style>
        #video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, 300px);
            grid-auto-rows: 300px;
        }

        .main {
            display: grid;
            grid-auto-columns: 300px;
        }

        video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
    </style>
    <script>
        const roomId = "{{ roomId }}"
        const userId = "{% if user %}{{ user.id }}{% else %}0{% endif %}"
        const userName = "{% if user %}{{ user.username }}{% else %}anonymous{% endif %}"
    </script>
    <script src="https://cdn.bootcss.com/flv.js/1.5.0/flv.min.js" defer></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="/script.js" defer></script>
{% endblock %}

{% block body %}
    <div class="main">
        <div>
            <video id="videoElement" controls="controls" autoplay="autoplay"></video>
        </div>
        <div class="chat">
            <input type="text" class="message"><button class="sendMsg">Wyślij</button>
        </div>
    </div>
{% endblock %}